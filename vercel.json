{
  "buildCommand": "cd frontend && npm install && npx ng build",
  "outputDirectory": "frontend/dist/app/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
