{
  "buildCommand": "cd frontend && npm install && npx ng build admin-app",
  "outputDirectory": "frontend/dist/admin-app/browser",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
